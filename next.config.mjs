/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['utfs.io', "aceternity.com", "www.behance.net"], // İzin verilen ana makineler
      },
};

export default nextConfig;
